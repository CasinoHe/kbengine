- use project's mysql source code
- use lib python, not system python to link the library
- use lib ssl, not system ssl
- fix entity_table_mysql.cpp compile warning
- cmake work with windows
- replace log4cxx by spdlog, simpler and faster